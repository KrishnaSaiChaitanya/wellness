'use strict';

/**
 * pre-test-requirement router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::pre-test-requirement.pre-test-requirement');
