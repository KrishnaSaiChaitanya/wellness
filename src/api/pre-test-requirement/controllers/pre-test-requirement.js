'use strict';

/**
 * pre-test-requirement controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::pre-test-requirement.pre-test-requirement');
